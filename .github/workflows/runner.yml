name: My First Workflow

on: push

jobs:
    demo-job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v4

        - name: List Current Folders
          run: ls

        - name: Display ReadME
          run: cat README.md

